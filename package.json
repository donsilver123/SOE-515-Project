{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "soe511",
  "scripts": {
    "dev": "pnpm --recursive --parallel dev",
    "build": "pnpm --recursive --parallel build",
    "typegen": "pnpm --recursive --parallel typegen",
    "deploy": "pnpm --recursive --parallel run deploy",
    "setup": "pnpm --recursive --parallel run setup"
  }
}
