{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "soe511",
  "scripts": {
    "dev": "pnpm --recursive --parallel dev",
    "typegen": "pnpm --recursive --parallel typegen",
    "deploy": "pnpm --filter @soe511/shared-contract run deploy && pnpm --filter @soe511/insurance-contract run deploy"
  }
}
